export interface DebtNetInterestType {
  region: string;
  option: string;
  period: string;
  percentages: [];
}
export interface CategoryData {
  category: string;
  description: string;
  source: number;
}
